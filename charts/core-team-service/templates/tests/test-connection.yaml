apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "core-team-service.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "core-team-service.name" . }}
    helm.sh/chart: {{ include "core-team-service.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "core-team-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
